interface:
  display_name: "Code Review"
  short_description: "Review code against accumulated design rules"
  default_prompt: "Use $code-review to review this change against the project design rules."
